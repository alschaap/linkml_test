id: https://example.org/derived_schema
name: derived_schema
prefixes:
  ex: https://example.org/
default_prefix: ex
default_range: string
imports:
  - base_schema

classes:
  Employee:
    is_a: Person
    description: A person employed by an organization.
    attributes:
      employee_id:
        identifier: true
        range: string
      department:
        range: string

